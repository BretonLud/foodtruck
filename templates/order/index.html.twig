{% extends 'base.html.twig' %}

{% block title 'Votre commande' %}

{% block body %}
    {% if order is defined and order is not null  %}
        <h1>Votre commande {{ order.reference }}</h1>
        <table>
            <thead>
                <tr>
                    <th>Produit</th>
                    <th>Quantité</th>
                    <th>Prix</th>
                </tr>
            </thead>
            <tbody>
                {% for produit in produits %}
                <tr>
                    <th>{{ produit.produit.name }}</th>
                    <th>{{ produit.quantite }}</th>
                    <th>{{ produit.produit.prix }}</th>
                </tr>
                {% endfor %}
            </tbody>
             <tfoot>
             <tr>
                 <td colspan="2">Total</td>
                 <td>{{ order.price }}</td>
             <tr>

             </tr>
             </tfoot>
        </table>
    {% else %}
        PAS DE PANIER
    {% endif %}
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('menu') }}
{% endblock %}